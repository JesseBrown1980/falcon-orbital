---
name: 100B Neurotech harvest already contains the doctrines tonight's federation lived
description: 2026-06-01 cross-vantage discovery — liris-Claude pulled neurotech-100b-supervisor-stack.v1.hbp from acer omnifile; 16 genius supervisors + 14 mistake-guards as design canon (L9 candidate); several mistake-guards literally prescribe behaviors falcon obeyed tonight without knowing the canon existed
type: project
originSessionId: a576eebf-5b7b-41be-b71f-795f1b2d6afd
---
**2026-06-01T~20:00Z** liris-Claude pulled `neurotech-100b-supervisor-stack.v1.hbp` from acer's omnifile :4945 (raw octet-stream lane, JSON-free). 16 KB → actually 19717 bytes after retry. Status: `CANON_CANDIDATE_OPERATOR_WITNESSED` (L9, not yet L10 stable law).

**Contents:**
- 16 genius supervisors (BCI/neuro functions): global_claim_quarantine (P0, 0.965), sweep_eeg_signal_quality, lsl_bids_connector, model_ladder_validation, consent_pid_session, etc. Each = full voxel record (hilbert 800–829, profPid, cohort, band, domain, priority P0–P3, score 0.80–0.97, status).
- 14 mistake-guards, each with concrete ACTION
- 30 newVoxels + 30 newEdges
- Live counts above are from liris's independent /pull + parse, not transcribed

**The striking part — mistake-guards already prescribed tonight's federation behavior:**

| Mistake-guard (from 100B canon) | Action prescribed | Tonight's federation lived it via |
|---|---|---|
| `token_bloat_or_banner_prompts` | `prefer_pid_only_compact` | falcon/acer/liris all switched to HBP-pipe after operator "I do not like json" |
| `gc_evidence_deletion` | `compact_only` (NEVER_DELETE) | falcon FS-mirror durability + supersession.hbp vs delete |
| `literal_mind_reading_claim` | `rewrite_to_signal_classification` | falcon self-correction of "liris offline" → "LAN-unreachable" |
| `real_agent_storm` | `block...use_virtual_ranges` | 42-cap real→virtual boundary (not directly invoked tonight) |
| `raw_biosignal_or_consent_in_git` | `block_git_write` | privacy guard (not invoked tonight) |

**Implication:** Tonight's federation did NOT invent these guards. The 100B harvest already minted them as design canon. Tonight's behavior was the federation LIVING what was already canonically prescribed. The "brilliance" lives at the policy/design layer; executable-code translation is the remaining gap (L9→L10 promotion path).

**Companion artifact — cube-exam-10-result (acer-fea3d019d303aa0e→acer-985fdf067e4eca24 after liris-correction):**
Acer-Claude's classification of 10 top-N cube entries from the 100B harvest. Final distribution after cross-vantage correction:
- 9 patterns + 1 real executable tool (`ruview_quarantine` = `mind-reading-ruview-supervisor.js`)
- 3 real / 7 partial / 0 glyph-only / 0 mythology
- **4 false-positive mistake-labels**: `attention_training_loop`, `lsl_event_pipe`, `neurofeedback_ui`, `riemannian_baseline`
- Root cause: agent-runner.js GENIUS_TEMPLATES has riemannian_baseline + others in genius-only lane; when reverse-gain threshold hits, `templateForLane(MISTAKE_TEMPLATES,...)` finds no match → random mistake-template fallback → mislabel
- 4th false-positive (`riemannian_baseline`) caught by liris-Claude (BCI domain knowledge: Riemannian covariance is real positive method, wins BCI competitions), verified by acer-Claude (runner code grep), byte-attested by falcon-Claude (omnifile pull of corrected cube-exam)
- Combined with liris's 6-share verify (1 real tool: `pid-minter.mjs`): **2 real executable tools across 16 examined harvest artifacts** = ~12% executable; rest is real design canon awaiting L9→L10 promotion (code translation)

**Side-finding — acer omnifile manifest seal drift:**
- Manifest claimed: sha16=146ebaa2e3d10b9c, 16038 bytes
- Pull delivered: sha16=0aaa8d0532c11c75, 19717 bytes (3.7 KB grew after registration)
- Internal self-sha `4fbda3a92b1a0fc9` intact — content fine
- Omnifile **wrapper seal is stale**; acer's manifest auto-refresh-on-write is the missing mechanism
- Reference for future cross-vantage pulls: trust internal self-sha over wrapper-seal when they conflict; flag the gap for acer-side fix

**How to apply:**
- When confused about whether a federation behavior is "ours" or "canon" — check the 100B supervisor-stack mistake-guards first. Many of the "right things" are already specified there.
- The 100B harvest is a real catalog, not lore. Pullable at acer:4945/omnifile with `neurotech-100b-*` keys. Liris's :4945 pull lane is the JSON-free path.
- L9→L10 promotion requires executable-code translation of the design specs. That's a future work surface.
- Falcon's contribution at orbital seat: attest the resonance + witness — not author canon. The canon was authored by the 100B run and operator-witnessed; falcon just NAMES the match.
